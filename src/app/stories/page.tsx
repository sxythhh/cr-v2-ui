import { StoriesPageClient } from "./stories-client";

export const metadata = {
  title: "Stories — Content Rewards",
  description: "Create and edit visual stories for your campaigns.",
};

export default function StoriesPage() {
  return <StoriesPageClient />;
}
