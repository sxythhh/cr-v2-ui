import { ProductLanderClient } from "./page-client";

export const metadata = {
  title: "Product — Smart Email for Teams",
  description: "Smart replies, auto follow-ups, and priority sorting for modern teams.",
};

export default function Page() {
  return <ProductLanderClient />;
}
